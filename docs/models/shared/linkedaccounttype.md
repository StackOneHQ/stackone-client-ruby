# LinkedAccountType

The account type

## Example Usage

```ruby
require "stackone_client"

value = LinkedAccountType::PRODUCTION

# Open enum: use .deserialize() to create instances from custom string values
custom = LinkedAccountType.deserialize("custom_value")
```


## Values

| Name             | Value            |
| ---------------- | ---------------- |
| `PRODUCTION`     | production       |
| `TEST`           | test             |
| `UNMAPPED_VALUE` | unmapped_value   |